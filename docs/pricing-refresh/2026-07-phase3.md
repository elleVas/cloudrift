# Pricing refresh — 2026-07 (Phase 3/3: database/app-service)

Date: 2026-07-28

Scope: `rds-gp2`, `rds-gp3`, `rds-io1`, `rds-io2`, `rds-standard`, `dynamodb-rcu`,
`dynamodb-wcu`, `aurora-acu`, `secretsmanager-secret`, `codepipeline-pipeline`
in `libs/cloud-cost/infrastructure/aws-adapter/src/pricing/prices.json`.

All prices verified against AWS's official Price List Bulk API
(`pricing.us-east-1.amazonaws.com`), used as the authoritative first-party
source because direct fetches to `aws.amazon.com/*/pricing/` pages were
blocked (HTTP 403) by this environment's outbound network policy.

## Keys changed

- `rds-gp2` (region `ap-southeast-1`): $0.131 → $0.138 /GB-month
- `rds-gp2` (region `ap-northeast-1`): $0.142 → $0.138 /GB-month
- `dynamodb-rcu` (region `eu-west-1`): $0.00013 → $0.000147 /RCU-hour
- `dynamodb-wcu` (region `eu-west-1`): $0.00065 → $0.000735 /WCU-hour
- `dynamodb-rcu` (region `eu-central-1`): $0.00013 → $0.0001586 /RCU-hour
- `dynamodb-wcu` (region `eu-central-1`): $0.00065 → $0.000793 /WCU-hour
- `dynamodb-rcu` (region `ap-southeast-1`): $0.00013 → $0.000148 /RCU-hour
- `dynamodb-wcu` (region `ap-southeast-1`): $0.00065 → $0.00074 /WCU-hour
- `dynamodb-rcu` (region `ap-northeast-1`): $0.00013 → $0.0001484 /RCU-hour
- `dynamodb-wcu` (region `ap-northeast-1`): $0.00065 → $0.000742 /WCU-hour

`pricesAsOf` bumped from `2025-06` to `2026-07`.

## Verified unchanged (left as-is)

- `rds-gp2` (`default`, `us-east-1`, `us-west-2`): confirmed still $0.115/GB-month.
- `rds-gp2` (`eu-west-1`): live API shows $0.127 vs stored $0.128 — a <1%
  difference judged to be trivial rounding noise, not a real price change;
  left unchanged per instructions to avoid updating on trivial deltas.
- `rds-gp2` (`eu-central-1`): live API shows $0.137 vs stored $0.138 — same
  trivial-rounding judgment as above; left unchanged.
- `rds-gp3`, `rds-io1`, `rds-io2`, `rds-standard` (`default` only — not present
  in any region table): confirmed unchanged at $0.115 / $0.125 / $0.125 /
  $0.100 per GB-month respectively.
- `dynamodb-rcu`, `dynamodb-wcu` (`default`, `us-east-1`, `us-west-2`):
  confirmed unchanged at $0.00013/RCU-hour and $0.00065/WCU-hour.
- `aurora-acu` (`default` only): confirmed unchanged, $0.12/ACU-hour ×730 =
  $87.60/month.
- `secretsmanager-secret` (`default` only): confirmed unchanged, $0.40/secret/month.
- `codepipeline-pipeline` (`default` only): confirmed unchanged, $1.00/active
  pipeline/month (V1 flat-fee SKU; noted that AWS also now offers a V2
  per-action-execution-minute pricing model alongside it, but no flat V2
  monthly SKU exists, so the V1 flat price remains the correct baseline for
  this key).

No keys in scope were left unverified this run — all were confirmed via a
live fetch against AWS's official Price List Bulk API.
