# Pricing refresh — 2026-07 (phase 1/3: storage)

Date: 2026-07-28

Scope: storage-related price keys only (`ebs-gp2`, `ebs-gp3`, `ebs-io1`, `ebs-io2`,
`ebs-st1`, `ebs-sc1`, `ebs-standard`, `ebs-snapshot`, `s3-standard`, `efs-standard`,
`cw-logs`, `ecr-storage`, `rds-manual-snapshot`) in
`libs/cloud-cost/infrastructure/aws-adapter/src/pricing/prices.json`. Network/FSx
and database/app-services keys are covered by separate phase 2/3 routines and were
not touched here.

Prices were verified against AWS's official Price List Query API
(`pricing.us-east-1.amazonaws.com`), the same machine-readable feed that backs the
public `aws.amazon.com/*/pricing/` pages, since the marketing pages themselves were
blocked by this session's egress policy.

## Keys changed

- `default.ebs-sc1`: 0.018 → 0.015 (Cold HDD storage, us-east-1 rate; prior value was stale)
- `eu-west-1.ebs-snapshot`: 0.053 → 0.050
- `eu-west-1.cw-logs`: 0.033 → 0.030
- `eu-west-1.s3-standard`: 0.024 → 0.023
- `eu-central-1.ebs-snapshot`: 0.053 → 0.054
- `eu-central-1.cw-logs`: 0.033 → 0.0324
- `eu-central-1.efs-standard`: 0.330 → 0.360
- `ap-southeast-1.ebs-gp2`: 0.114 → 0.120
- `ap-southeast-1.ebs-io1`: 0.131 → 0.138
- `ap-southeast-1.ebs-snapshot`: 0.055 → 0.050
- `ap-southeast-1.cw-logs`: 0.033 → 0.030
- `ap-southeast-1.efs-standard`: 0.340 → 0.360
- `ap-northeast-1.ebs-snapshot`: 0.053 → 0.050
- `ap-northeast-1.efs-standard`: 0.340 → 0.360

`pricesAsOf` bumped from `2025-06` to `2026-07`.

## Keys checked, left unchanged (already current)

- `default`: `ebs-gp2`, `ebs-gp3`, `ebs-io1`, `ebs-io2`, `ebs-st1`, `ebs-standard`,
  `ebs-snapshot`, `cw-logs`, `s3-standard`, `efs-standard`, `ecr-storage`,
  `rds-manual-snapshot` — all matched current AWS us-east-1 rates.
- `us-east-1` table and `us-west-2` table: all in-scope keys (`ebs-gp2`, `ebs-gp3`,
  `ebs-snapshot`, `cw-logs`, `s3-standard`, `efs-standard`) matched current rates,
  no changes needed.
- `eu-west-1.ebs-gp2`, `eu-west-1.ebs-gp3`, `eu-west-1.efs-standard` — matched.
- `eu-central-1.ebs-gp2`, `eu-central-1.ebs-gp3`, `eu-central-1.s3-standard` — matched.
- `ap-southeast-1.ebs-gp3`, `ap-southeast-1.s3-standard` — matched.
- `ap-northeast-1.ebs-gp2`, `ap-northeast-1.ebs-gp3`, `ap-northeast-1.cw-logs`,
  `ap-northeast-1.s3-standard` — matched.

No keys in scope were left unverified/uncertain this run — all values were resolved
via unambiguous, single-SKU entries in AWS's official Price List API data (dated
2026-07-08 through 2026-07-28, i.e. current at time of writing).
